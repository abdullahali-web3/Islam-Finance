# Copy to `.env` for local `vercel dev`, and set the same var in the Vercel/Cloudflare dashboard for
# production. NEVER commit a real key — `.env` is gitignored. See ADR 0004 + 0008.
METAL_PRICE_API_KEY=your_provider_api_key_here
